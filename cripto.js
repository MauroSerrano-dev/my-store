/* const crypto = require("crypto");

// Gerar uma chave secreta aleatória (exemplo com 32 bytes de entropia)
const secretKey = crypto.randomBytes(32).toString("hex");

console.log("Secret Key:", secretKey); */

/* const jwt = require('jsonwebtoken');

const secretKey = 'SuaSecretKeySuperSecreta'; // Substitua pela sua secret key

function generateToken(payload) {
    return jwt.sign(payload, secretKey);
}

// Exemplo de uso:
const token = generateToken({ store: 'my-store' });
console.log('Token JWT sem expiração:', token); */
